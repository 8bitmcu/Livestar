name: Build & Release Android APK

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      # 1. Setup Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      # 2. Install Android NDK and CMake
      - name: Setup Android NDK
        uses: android-actions/setup-android@v3
        with:
          # Installs NDK. If your project requires a specific NDK version, 
          # specify it here (e.g., ndk-version: '25.2.9519653')
          ndk-version: 'latest'
          cmake-version: '3.22.1'

      # 3. Install Rust Toolchain & Cross-Compilation Targets
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: |
            aarch64-linux-android
            armv7-linux-androideabi
            i686-linux-android
            x86_64-linux-android

      # 4. Cache Cargo dependencies to speed up subsequent builds
      - name: Cache Cargo Registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Grant Execute Permission to Gradle Wrapper
        run: chmod +x gradlew

      # 5. Build the Release APK
      # The NDK environment variable is explicitly passed to ensure Gradle/Cargo bridges can locate it.
      env:
        ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
      run: ./gradlew assembleRelease

      - name: Generate Release Tag
        id: tag
        run: echo "release_tag=build-$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.release_tag }}
          name: Release ${{ steps.tag.outputs.release_tag }}
          draft: false
          prerelease: false
          files: app/build/outputs/apk/release/app-release-unsigned.apk
